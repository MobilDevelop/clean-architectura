import 'package:flutter/material.dart';

final class OutputsPage extends StatelessWidget {
  const OutputsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Chiqimlar oynasi")),
    );
  }
}